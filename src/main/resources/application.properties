server.port=8080
spring.application.name=signature-docx-poc

# Gotenberg service URL (for local development, use localhost)
gotenberg.url=http://localhost:3000

# File upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.com.example.docxpoc=DEBUG
